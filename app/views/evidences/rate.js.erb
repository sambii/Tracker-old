
// replace the body of the modal dialog box with the erb we want rendered
// $('#modal_content').html("<%= escape_javascript(render('evidences/rate_evidences') ) %>");
$('#modal-body').html("<%= escape_javascript(render('evidences/rate_evidences') ) %>");
